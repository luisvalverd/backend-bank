const { Router } = require("express");
const router = Router();

router.post("/login", (req, res) => {
  // login user created
});

router.post("/register", (req, res) => {
  // register new user
});
